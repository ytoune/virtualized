import type { HTMLElement } from './interfaces'

// export type VirtualizedProps = Readonly<{
//   divRef: () => HTMLElement | null
// }>
export const createVirtualized = (divRef: () => HTMLElement | null) => {
  // pass
}
