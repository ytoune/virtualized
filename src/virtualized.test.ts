import { describe, it, expect } from 'vitest'
import { createVirtualized } from './virtualized'

describe('virtualized', () => {
  it('ok', () => {
    createVirtualized
    expect
    window.scroll
  })
})
